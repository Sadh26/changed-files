name: Node CI

on: 
  push:

  workflow_dispatch:

jobs:
  build_and_test:
    runs-on: self-hosted
    steps:

      - name: Archive production artifacts
        uses: actions/upload-artifact@v3
        with:
          name: test report
          path: |
            C:\Users\sadhana.suresh\Desktop\Changed files\changed-files\src\CelsiustoFarenheit\Tests\Analysis.txt
            

      # - name: Archive code coverage results
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: code-coverage-report
      #     path: output/test/code-coverage.html
